# Vercel
.vercel

# Env / secrets (но .env.example — шаблон, его коммитим)
.env
.env.local
.env.*.local
*.env
!.env.example

# Service-account / credential files
credentials.json
*.pem
*.key
*.p12

# OS / editor cruft
.DS_Store
